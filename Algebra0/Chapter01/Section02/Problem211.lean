/-
In view of \cref{exr:fn-set-cardinality}, it is not unreasonable to use $2^A$ to
denote the set of all functions from an arbitrary set $A$ to a set with $2$
elements (say $\set{0, 1}$). Prove that there is a bijection between $2^A$ and
the power set of $A$.
-/
